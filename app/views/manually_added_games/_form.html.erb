<%= form_with(model: manually_added_game, local: true) do |form| %>
  <% if manually_added_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manually_added_game.errors.count, "error") %> prohibited this manually_added_game from being saved:</h2>

      <ul>
        <% manually_added_game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
