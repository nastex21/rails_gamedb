<p id="notice"><%= notice %></p>

<h1>Created Games</h1>

<table>
  <thead>
    <tr>
      <th>Game</th>
      <th>Title</th>
      <th>Image</th>
      <th>System</th>
      <th>Released date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @created_games.each do |created_game| %>
      <tr>
        <td><%= created_game.game_id %></td>
        <td><%= created_game.title %></td>
        <td><%= created_game.image %></td>
        <td><%= created_game.system %></td>
        <td><%= created_game.released_date %></td>
        <td><%= link_to 'Show', created_game %></td>
        <td><%= link_to 'Edit', edit_created_game_path(created_game) %></td>
        <td><%= link_to 'Destroy', created_game, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Created Game', new_created_game_path %>
