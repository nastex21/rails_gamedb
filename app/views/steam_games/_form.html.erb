<%= form_with(model: steam_game, local: true) do |form| %>
  <% if steam_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(steam_game.errors.count, "error") %> prohibited this steam_game from being saved:</h2>

      <ul>
        <% steam_game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
